#include "shell.h"

/**
* custom_prompt - entry point
* Return: Always 0 on success
*/

void custom_prompt(void)
{
	/* Define */
	char *prompt = "♥ ";
	/* Print the prompt */
	printf("%s", prompt);
}
